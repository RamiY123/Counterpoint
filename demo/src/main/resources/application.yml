spring:
  profiles:
    active: dev
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:sk-placeholder-will-be-set-at-runtime}
      chat:
        options:
          model: gpt-4o
          temperature: 0.7

app:
  debate:
    max-response-words: 100
